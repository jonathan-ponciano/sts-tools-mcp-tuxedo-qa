node_modules/
dist/
test-results/
results/

# local, personal permissions — not the agent definitions in .claude/agents/
.claude/settings.local.json

# per-project namespaces (TUXEDO_QA_PROJECT) — each holds its own
# tests/config/results, never shared across installs
projects/

# per-business runtime data — never commit real credentials/config
config/*.json

# business-specific test helpers/fixtures (internal URLs, tokens)
tests/helpers/fretebras.ts
tests/*.spec.ts
inspect.cjs
inspect-live.cjs
inspect-empresas.cjs

*.log
.DS_Store
